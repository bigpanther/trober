package models_test

// AUTOGENERATED BY: HSM GEN

import(
	"testing"

	m "{{.PackageName}}"
)

func TestIsValid{{.Key}}(t *testing.T) {
	var validVal = "{{index .Schema.Enum 0}}"
	var inValidVal = "_someInvalidval_"
	if !m.IsValid{{.Key}}(validVal) {
		t.Fatalf("IsValid{{.Key}}(%q) should be true", validVal)
	}
	if m.IsValid{{.Key}}(inValidVal) {
		t.Fatalf("IsValid{{.Key}}(%q) should be false", inValidVal)
	}
}
